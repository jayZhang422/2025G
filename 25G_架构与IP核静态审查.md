# 25G 架构与 IP 核静态审查

审查日期：2026-07-21  
审查范围：`E:\7020_Project\25G\25G_PS`、`E:\7020_Project\25G\25G_PL` 的 AGENTS.md、`.ai`、Doc，以及 `25G_PL/ip_core` 中的两个自定义 IP。  
审查方式：源代码、IP-XACT `component.xml` 和现有顶层连线的静态检查；**未运行综合、实现或仿真**。

## 结论摘要

1. 当前工程仍是上一题的 Zynq-7020 架构：AD9226 采样经异步 FIFO、AXIS FIFO、S2MM DMA 送入 DDR；PS 分析后经 AXI BRAM 写入十字控制表；PL 的双 DDS 驱动 AD9767 双通道 DAC。
2. 这套架构可复用为 G 题的数字主干，但不能只靠原有代码完成 G 题。必须增加未知电路学习、任意周期波形等效生成、ADC 高输入阻抗模拟前端、DAC 输出幅度校准/放大等内容。
3. `ad_fifo_ip` 内的 `fifo.v` 与旧版完全相同；`ad9226.v` 则有意删除了 PLL、`locked` 门控、65 MHz 输出和 0 度 ADC 时钟输出。因此它可复用采样加 CDC 功能，**不能无修改直接等价替换**旧 `ad9226.v + fifo.v`。
4. `DDS_DAC_ip` 保留了旧 `ad9767.sv` 的十字控制表、影子寄存器、`COMMIT_SEQ` 原子提交、双通道相位累加、幅度缩放、停止时中码和下降沿更新 DAC 数据等主干逻辑。它把正弦/三角 ROM 外移，并新增任意波形 RAM 端口，方向适合 G 题；但当前顶层没有这些 ROM/RAM 连线，故**不能不改顶层/BD 就替换**旧文件。
5. 两个 IP 可以替代原来的三个 RTL 文件（`ad9226.v`、`fifo.v`、`ad9767.sv`），但仅在完成本文列出的时钟、ROM/RAM、顶层实例化和约束调整后。替换后不能表述为“完全一样”：旧的二波形 DDS 功能可等价保留；ADC 的 PLL 锁定/物理时钟输出和 DAC 的内部 ROM 依赖已移至 IP 外部。

## 当前架构概览

```text
AD9226[11:0]
  -> 5.1208 MHz 相移采样
  -> 12-bit 至 16-bit 异步 FIFO
  -> 100 MHz AXIS，4096 beat/TLAST
  -> AXIS FIFO -> AXI DMA S2MM -> PS DDR

PS 软件
  -> DMA 采样/频率与波形分析
  -> AXI BRAM 十字控制表（最后写 COMMIT_SEQ）
  -> 125 MHz PL DDS
  -> AD9767 A/B DAC
```

现有硬件合同中，DMA 为 16-bit、simple mode、仅 S2MM、轮询完成；DDS 控制表位于 BRAM，控制字包含 A/B 波形、步进、相位、幅度、RUN、PHASE_RELOAD、B 相位增量和提交序号。当前 PS 应用的状态机仍服务于上一题的双分量识别，而非 G 题。

## G 题方案判断

### 可直接复用的部分

| 现有部分 | G 题用途 | 结论 |
| --- | --- | --- |
| 125 MHz DDS + AD9767 | 100 Hz 步进到至少 1 MHz 的正弦激励 | 可复用。1 MHz 远低于 DAC 125 Msps 更新率。 |
| AD9226 + DMA 到 DDR | 学习阶段采集未知网络的输出；运行阶段采集外部周期输入 | 可复用数字链路。5.1208 Msps 对 50 kHz 基波有充足采样率。 |
| PS DDR、DMA、CMSIS DSP | 扫频增益测量、滤波类型判断、FFT/DFT、重采样和 IFFT | 可复用，但旧识别算法不可直接复用。 |
| PS 写 BRAM 的原子提交 | 设定正弦频率/幅度、启动/停止、相位控制 | 可复用。PS 必须继续保持 `COMMIT_SEQ` 最后写入。 |
| 双 DAC | 一路作为正式输出，另一路可作学习激励、调试或预留 | 可复用。AD9767 的 B 通道模拟反相补偿仍应由 PS 配置保持。 |
| 新 DDS IP 的任意波形 RAM 接口 | 发挥部分对方波/其他周期波形生成等效输出 | 是新增且关键的能力。 |

### 必须新增或重做的部分

| 需求 | 现状不足 | 推荐实现 |
| --- | --- | --- |
| 已知 RC 模型板 | FPGA 工程不含模拟 RC 有源滤波器 | 独立制作并实测标定该模拟板。 |
| 基本要求的输出幅度 | DAC 模块手册标称最高约 7.5 Vpp，且依赖电位器与负载 | 建立 DAC 码值到 Vpp 的实测标定表；为高衰减点配置外接低失真增益级。按题给传函，3 kHz 时 `|H|` 约为 0.161，若要求 2 Vpp 输出则需要约 12.4 Vpp 输入，现有 DAC 模块的标称 7.5 Vpp 不足。 |
| 未知 RLC 学习 | 当前 PS 是旧题双信号识别，且仅有 4096 样本帧 | 扫频输出正弦，采集每点稳态幅值；用响应曲线判定低通/高通/带通/带阻，并拟合传递函数。为最低 1 kHz 输入获取完整周期，至少采 5120 点，建议 DMA 帧改为 8192 点或连续拼接。 |
| 发挥部分等效输出 | 原 DDS 仅有正弦/三角；当前 BRAM 只存控制表 | 将新 DDS IP 的 `arb_*` 接到新增的 PS 可写、DDS 可读的双口波形 RAM。采集输入的一个完整周期，频域乘以拟合的 `H(jw)` 后 IFFT，写入 4096 点波表，以 DDS 相位步进重放。 |
| 输入阻抗不低于 100 kOhm | RTL 无法保证 AD9226 前端阻抗 | 在 ADC 前增加高输入阻抗、带宽满足 50 kHz 的缓冲/偏置/保护模拟前端；以实测为准。 |
| 连续同频稳定显示 | 无相位要求，但频率必须无漂移 | 对外部输入估计基波频率并设置相同 DDS 相位步进；不要让逐帧噪声直接连续改写输出频率。 |

建议把发挥部分的学习和运行拆成两个明确状态：

```text
LEARN: DAC 扫频 -> ADC 采样 -> 估计 |H(f)| -> 分类/拟合/保存模型
RUN:   ADC 捕获外部周期输入 -> 周期和频率估计 -> 频域滤波/重采样
       -> 写任意波表 -> DDS 以同频率连续重放
```

单 ADC 通道可以用于学习阶段：激励源由自身 DAC 已知，ADC 采未知网络输出即可估计幅频响应。但模拟输入链路必须满足阻抗、偏置、量程和保护要求。是否能达到“无失真”的最终评分仍取决于未知网络、模拟前端、DAC 放大器和波表长度，不能由 RTL 静态检查保证。

## IP 核逐项检查

### 1. `ad_fifo_ip`

公开顶层为 `ad_fifo_warpper`（目录及模块均为此拼写），输入为 `rst_n`、`clk_phase`、`adc_din`、`rd_clk`、`rd_en`，输出为 `dout[15:0]`、`empty`。

| 项目 | 旧文件 | IP 内容 | 静态判断 |
| --- | --- | --- | --- |
| FIFO 数据路径 | `fifo.v` | `src/fifo.v` | SHA-256 相同，功能完全一致。 |
| ADC 稳定等待 | PLL lock 后 64 个相移采样时钟 | 64 个 `clk_phase` 周期 | 保留 64 周期等待，但删除了 `locked` 条件。 |
| ADC 0 度时钟 | 旧 `ad9226` 内部 PLL 产生并输出 `clk_ad` | IP 没有该端口 | 不能直接等价替换；顶层/BD 必须另提供同一 PLL 的 0 度输出到 AD9226 时钟引脚。 |
| 相移采样时钟 | 旧模块内部 PLL 产生 `clk_ad_deg` | 外部输入 `clk_phase` | 可以复用，但 `clk_phase` 必须是同一 PLL 的 5.1208 MHz、约 181.8 度输出。 |
| PLL 锁定保护 | `!locked` 时保持采样失效 | IP 不含 `locked` | 外部必须在 PLL 未锁定时保持 `rst_n=0`；否则启动语义不同。 |
| FIFO IP 依赖 | 工程内 `fifo_generator_0.xci` | 包中含 `src/fifo_generator_0/fifo_generator_0.xci` 并已登记 | 打包内容完整，作为 Vivado 2020.2 的 Zynq 用户 IP 可用。 |

结论：ADC/FIFO IP 的“数据采集加跨时钟 FIFO”封装正确，且删除内部时钟资源有利于避免重复 PLL；但它不再是 ADC 时钟生成模块。最小接入是：在顶层或 BD 放置唯一 PLL，0 度时钟接 ADC 管脚，181.8 度时钟接 `clk_phase`，锁定信号参与复位释放，然后以 `ad_fifo_warpper` 替换旧的两个实例。

### 2. `DDS_DAC_ip`

公开顶层为 `DAC_DDS_Output`，控制 BRAM 端口和 DAC A/B 输出端口均保留；正弦、三角、任意波形改为六组独立的地址/数据端口。

| 项目 | 旧 `ad9767.sv` | 新 IP | 静态判断 |
| --- | --- | --- | --- |
| 十字 BRAM 控制表 | 10 个 32-bit 字，地址 0x00 至 0x24 | 相同 | 可与现有 PS `dds_control` 协议兼容。 |
| 原子提交 | 影子寄存器、完整扫描、`COMMIT_SEQ` 应用 | 相同 | 主干逻辑保留。 |
| A/B DDS、幅度、停止中码 | 有 | 有 | 主干行为保持。 |
| B 通道相位增量 | 控制位 2 | 相同 | 保持；PS 对 AD9767 B 通道模拟反相的补偿也必须保留。 |
| 正弦/三角 ROM | 核内例化四个 ROM | 外部端口 | 需要在 BD 中为 A/B 各自提供读端口；使用单端口 ROM 时是四个 ROM，或使用双端口 ROM 时需正确分配两个读端口。 |
| 任意波形 | 无 | `arb_addr/data_{a,b}`，波形值 2 选择 | 是 G 题发挥部分所需能力。 |
| ILA 例化 | 旧源码含 `ila_0` 实例 | 无 | 去除调试 IP 依赖，有利于通用封装。 |
| 数据更新时序 | DAC 数据在 `negedge clk` 更新，`da_wrt_* = clk` | 相同 | 仍依赖顶层转发 DAC CLK/WRT 的既有 ODDR/约束方案。 |

结论：在固定的 `PHASE_WIDTH=32`、`ADDR_WIDTH=12`、`DATA_WIDTH=14` 配置下，旧的正弦/三角 DDS/DAC 主功能可保留，新结构更适合接入 G 题的任意波表。

但其“通用性高”的结论只能给出**部分成立**：ROM/RAM 外移、任意波形和去除 ILA 是正确方向；而参数接口并未真正完全参数化，`shadow_step_*`、幅度、缩放右移、DAC 中码和最大码仍实质固定为 32/14-bit，`component.xml` 也只声明支持 `zynq`。因此应锁定当前三个默认参数使用，或以后再把内部位宽计算和 IP 支持族一并泛化，不能仅改 GUI 参数就假定功能正确。

## 是否可用两个 IP 替代三个文件

| 替代目标 | 可否替代 | 前提 |
| --- | --- | --- |
| `ad9226.v + fifo.v` -> `ad_fifo_warpper` | 可以，但非直接掉入式替换 | 复用/外置唯一 PLL；补回 AD9226 0 度时钟输出；把 PLL lock 纳入复位释放；修改 `H_top` 端口与实例。 |
| `ad9767.sv` -> `DAC_DDS_Output` | 可以，但非直接掉入式替换 | 在 BD/顶层连接控制 BRAM、四路正弦/三角 ROM、需要时的一/两路任意波 RAM，以及既有 DAC CLK/WRT 转发。 |
| 三个旧文件 -> 两个 IP | 可以作为重构目标 | 完成以上集成工作后，再进行编译、行为仿真和板级验证。 |

因此，对“稍微修改后是否完全一样”的准确答案是：

- **旧的 ADC -> FIFO -> DMA 数据通路可以保持等效**，但外置 PLL 的时钟频率、相位和复位释放必须保持原合同。
- **旧的正弦/三角双 DDS 输出可以保持等效**，但必须用同内容和相同读延迟的外部 ROM 连接新 IP。
- **全工程功能不可能字面完全一样**，因为新结构删除了 IP 内 PLL/lock/ROM/ILA，并增加了任意波形接口；这是有目的的结构重分配，不是逐行等价封装。

## 接入前必须检查的清单

1. PLL 只保留一个实例；确认 0 度 ADC 时钟和相移采样时钟同源，且 ADC 管脚的时钟约束仍存在。
2. 将 PLL `locked` 同步后纳入 ADC IP 的复位释放，不能仅把顶层 `i_rst` 直接接到 `rst_n`。
3. 用 `ad_fifo_warpper` 后，确保 AXIS 的 `tvalid/tready/tlast` 逻辑仍留在 100 MHz 域，TLAST 仍为每 4096 个已接受 beat 一次。
4. 给 DDS IP 的每个正弦/三角地址端口提供匹配的存储器读端口；确认 RAM/ROM 输出读延迟与旧 IP 一致。
5. 为 G 题任意波形至少增加一路 4096 x 16-bit、PS 可写且 DDS 可读的双口 RAM。现有 8 KiB 控制 BRAM 不应同时承担完整 4096 点波表和控制表。
6. 维持 `COMMIT_SEQ` 最后写入、DAC B 通道反相补偿、顶层 ODDR 转发时钟/WRT 和既有 DAC 时序约束。
7. 在逻辑确认后再依次做：编译检查、旧正弦/三角回归、任意波 RAM 回归、DMA 连续帧/8192 点采集回归、板级幅度与输入阻抗测试。

## 本次边界

本次未修改 RTL、BD、约束、PS 代码、IP 打包文件，也未运行综合、实现或仿真。上述“可替代”结论仅表示源代码级的可行集成路径，不替代这些后续验证。
