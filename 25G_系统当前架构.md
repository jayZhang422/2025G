# 25G 系统当前架构

## 范围与结论

本文件记录 `25G_PS` 与 `25G_PL` 的实际软硬件边界，以当前 RTL、IP 封装、Block Design 和 PS 应用源码为准。

**IQ 功能已放在 PL 端实现，且类型是 IQ 解调（数字下变频/锁相检测），不是 IQ 调制。**

- PL 的 `iq_demodulator_0` 在 ADC 约 5.12006 MHz 采样时钟域内，以 DDS 产生的正交 sin/cos 对 ADC 样本相乘并按窗口累加，输出 I/Q 结果。
- PS 通过 AXI-Lite 写 IQ 配置、读取稳定结果快照；PS 不执行 IQ 乘法和窗口累加。
- IQ 解调不会取代 DMA+FFT。DMA+PS 仍用于未知频率搜索、双分量分离、正弦/三角波判别和诊断；IQ 适用于已知频点的幅相/扫频测量。
- 现有 DAC 路径只有双单音 DDS BRAM 控制接口，没有持续 I/Q 基带样本流、插值或成形滤波，所以工程中没有 IQ 调制器。

## 顶层分层

```text
Zynq-7020
|- PS: Cortex-A9 + FreeRTOS
|  |- S2MM DMA 接收 ADC 原始帧到 DDR
|  |- CMSIS-DSP FFT、候选频率/波形分析与锁定状态机
|  |- AXI BRAM 控制双通道 DDS/DAC
|  `- AXI-Lite 配置并读取 PL IQ 解调器
`- PL: FPGA Fabric
   |- AD9226 采样、跨时钟 FIFO、AXIS 和 S2MM DMA 数据面
   |- iq_demodulator：ADC 域 IQ 解调与 AXI-Lite/IRQ 结果接口
   `- DDS_DAC：BRAM 提交协议、双通道 DDS、ROM 查表、AD9767 输出
```

## 输入采集和频率识别

```text
AD9226[11:0]
 -> ad_fifo_output/ad9226：相移 ADC 时钟寄存样本，稳定 64 拍后有效
 -> ad_fifo_output/fifo：5.12006 MHz 写入、100 MHz 读取，编码为 {ADC[11:0], 4'b0}
 -> H_top AXIS：每 4096 个接受样本标记 TLAST
 -> axis_data_fifo_0 -> axi_dma_adc（S2MM） -> HP0 DDR
 -> PS：缓存失效 -> 4096 点 RFFT -> 候选频率/波形联合残差选择
```

DMA 是 simple-mode、仅 S2MM、轮询完成，当前没有 DMA 中断连线。PS 只能在 DMA 完成后对接收缓冲区执行缓存失效，再读取样本。

## IQ 解调路径

```text
ad_fifo_output 已寄存 adc_raw + sample_valid（ADC 域）
 -> H_top -> top -> system_wrapper -> iq_demodulator_0
    |- dds_iq_lo：32 位 PINC/POFFSET，16 位 signed sin/cos，固定 8 拍延迟
    |- ADC 去中码、与 cos/sin 相乘、48 位窗口累加
    `- 结果快照/序号经 CDC 到 FCLK0 AXI-Lite，RESULT_PENDING 驱动 IRQ_F2P
 -> PS：读 I/Q、样本数与 RESULT_PINC；计算幅值和 atan2 相位；W1C 确认结果
```

IQ 配置基址当前为 `0x43C0_0000`，但 PS 应用必须使用 Vitis `xparameters.h` 的 `XPAR_IQ_DEMODULATOR_0_BASEADDR` 宏，不能写死地址。配置顺序为：写 `PINC`、`POFFSET`、`WINDOW`（及可选扫频参数），再写 `CTRL.COMMIT`；读完整结果后向 `STATUS.RESULT_PENDING` 写 1 确认。确认前 IP 不启动下一窗口，避免覆盖未读取快照。

## DDS/DAC 输出路径

```text
PS M_AXI_GP0 -> AXI Interconnect -> AXI BRAM Controller
 -> blk_PS_TO_PL 双口 BRAM（Port A: PS，Port B: 125 MHz DAC）
 -> DDS_DAC：扫描 10 个控制字、shadow、COMMIT_SEQ 原子应用
 -> A/B 独立相位累加器 -> 正弦/三角/任意波形 ROM
 -> 幅度缩放和 14 位饱和 -> AD9767 A/B
```

PS 必须先写 A/B 的波形、步进、相位、幅度和控制字，最后写 `COMMIT_SEQ`。`RUN` 为 0 时 DAC 输出中点；`PHASE_RELOAD` 以绝对相位重装；`B_PHASE_ADJUST` 将 B 相位字解释为带符号增量。

## PS 软件任务与状态

FreeRTOS 入口 `identification_main.c` 只创建 `signal_separator_task`。任务初始化 IQ 驱动后复用归档的裸机分离主流程。

```text
ARMED -- KEY1 --> LOCKING
LOCKING -- 连续 3 帧同解 --> RUNNING
LOCKING -- RESET 或 18 秒超时 --> ARMED
RUNNING -- T17/R17 --> B 相位 +/- 5 度
RUNNING -- N16/RESET --> 停止 DDS，回到 ARMED
```

锁定后不会用后续噪声自动更新 DDS 频率。IQ 报告目前对锁定的 A/B 频率各触发一次测量并打印原始幅值/相位，主频率与波形判定仍由 DMA+FFT 结果决定。

## 时钟域与维护规则

| 时钟域 | 当前频率 | 主要单元 |
| --- | ---: | --- |
| ADC 相移采样时钟 | 约 5.12006 MHz | AD9226 寄存、ADC FIFO 写端、IQ DSP、IQ DDS |
| PS FCLK0 | 100 MHz | FIFO 读端、AXIS、DMA 控制、AXI-Lite、IQ 结果寄存器 |
| DAC 时钟 | 125 MHz | DDS_DAC、波形 ROM、AD9767 |

改变 ADC PLL 实际频率时，必须同步更新 `dds_iq_lo` 的时钟元数据、PS 的 IQ PINC 换算常量、相关 BD 输出产物和本文档；不能将 DAC 的 125 MHz PINC 直接用于 ADC 域 IQ DDS。

## 维护入口

- PL 总架构：`25G_PL/.ai/ARCHITECTURE.md`
- PL IP 清单与 IQ 协议：`25G_PL/.ai/ip_rep.md`
- PS 软件架构：`25G_PS/.ai/architecture.md`
- PS 函数与算法说明：`25G_PS/Func/`
- 自定义 IP 使用说明：各 `25G_PL/ip_core/*/usage/README.md`

本文件描述当前结构，不代表行为仿真、时序收敛或板级幅相标定已经全部完成。
